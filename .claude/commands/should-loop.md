# should-loop

Use the project skill at `.claude/skills/looppilot/SKILL.md`.

This command is only a shortcut alias for LoopPilot. It must not duplicate qualification rules, decision schema, or contract templates.

Suggested invocation:

```text
Use the looppilot skill to decide whether this task should loop. Output schema-valid JSON first, then explain the decision. If safe, render the LoopPilot contract before taking action.
```
